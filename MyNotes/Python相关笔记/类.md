# 类（Class）

```python
class ab(ccc):   #定义class ccc为继承类的名字
    if __name__="__main__":   
   #表示每个py程序的入口，执行从这里开始 __name__是当前包的名称，而“__main__”是字符串，它是当前程序运行时包的值，所以当程序运行的时候，__name__就等于__main__
	#而当别的程序import导入该class类的时候，当前__name__的值时该类的名字，所有__name__不等于__main__,由此得出，if __name__="__main__"只做于当前程序测试来用
        
    
```

